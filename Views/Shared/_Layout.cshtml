<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    <link href="~/css/Site.css" rel="stylesheet" />
</head>
<body>
    <div>
        <nav>
            <a asp-controller="Home" asp-action="Index">首页</a>
            <a asp-controller="AdminPanel" asp-action="Edit">创建</a>
            <a asp-controller="AdminPanel" asp-action="Index">管理</a>
            @if (User.Identity.IsAuthenticated)
            {
                <a asp-controller="Auth" asp-action="Logout">注销</a>
            }
            else
            {
                <a asp-controller="Auth" asp-action="Login">登录</a>
            }
        </nav>
    </div>
    <div>
        @RenderBody()
    </div>
</body>
</html>
